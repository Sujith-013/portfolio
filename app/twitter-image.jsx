// Twitter's card image reuses the same design — re-exported rather than
// duplicated per Next's file-convention pattern for sharing an OG image.
export { default, size, contentType, alt } from "./opengraph-image";
